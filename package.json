{
  "name": "array-buffer-stream",
  "version": "0.1.0-a",
  "description": "make an ArrayBuffer readable/writable like a stream",
  "main": "./lib/index.js",
  "module": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "files": ["./lib"],
  "scripts": {
    "build": "rm -f ./lib/** && npx tsc && npm pack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "arraybuffer",
    "stream"
  ],
  "author": "Olivier Stuker",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
